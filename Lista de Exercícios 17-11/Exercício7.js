// Função que retorna o número de caracteres de uma string
function contarCaracteres(str) {
    return str.length;
}

// Exemplo de uso da função
let minhaString = "Hello, world!";
let numeroDeCaracteres = contarCaracteres(minhaString);
console.log(`A string "${minhaString}" tem ${numeroDeCaracteres} caracteres.`);
